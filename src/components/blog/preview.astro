---
import type { Post } from '@data/post';
import { toCapitalCase } from 'src/utils';
const post: Post = Astro.props.post
---

<div class="py-6 flex flex-row-reverse gap-6 md:gap-10 items-start">
	{post.img &&
	<a href={`/${post.slug}`} class="shrink-0">
		<img src={post.img as any} alt={post.title} width="160" height="160"
			class="w-20 h-20 md:w-40 md:h-40 rounded bg-base-200 object-cover" />
	</a>
	}
	<div class="indicator w-full">
		<span class="indicator-item  indicator-end badge badge-ghost">{ toCapitalCase(post.lang ?? "en") }</span>
		<div class="flex flex-col gap-4 w-full">
			<h2 class="text-2xl font-bold">
				<a class="hover:underline" href={`/${post.slug}`}>{post.title}</a>
			</h2>
			<p class="text-sm text-base-content/70">{post.desc}</p>
		</div>
	</div>
</div>